const maisComprados = document.querySelector(".maisComprados");

function ordenacao(valor) {
    if (valor == "data") {
        //query da data aqui
        alert("data")
    }
    else if (valor == "ranking") {
        //query do ranking aqui
        alert("ranking")
    }
    else {
        alert("Modo de ordenação não selecionado!")
    }
};